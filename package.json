{
  "name": "root",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap -- --hoist",
    "playground:dev": "lerna run --scope playground dev"
  },
  "devDependencies": {
    "lerna": "^6.6.1",
    "vue": "3.2.0"
  }
}
